# OpenAI Configuration (Option 1: Recommended)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Ollama Configuration (Option 2: Free, Local)
# Uncomment these lines if you want to use Ollama instead of OpenAI
# USE_OLLAMA=true
# OLLAMA_BASE_URL=http://localhost:11434

# Note: If neither is configured, the system will use simple text extraction
